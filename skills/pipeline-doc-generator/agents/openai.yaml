# GPT-6 適配變更說明：預設提示詞只描述交付目標；自主執行與授權規則由 Skill 和項目 AGENTS.md 統一管理，保留既有啟用策略。
interface:
  display_name: "Pipeline Document Generator"
  short_description: "Generate editable DataEngine waterline documents"
  default_prompt: "Use $pipeline-doc-generator to generate or revise a concise DataEngine waterline document from my PRD or HLD and deliver the validated Markdown, HTML, and PDF bundle using available evidence and existing authorization."
policy:
  allow_implicit_invocation: true
